# Local dev: point directly at the backend dev server.
# Leave empty (or remove this file) when building the Docker image or deploying same-origin behind nginx/Vercel rewrites.
VITE_API_BASE_URL=http://localhost:8000
